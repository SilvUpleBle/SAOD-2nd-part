#pragma once
#include <iostream>
static class utils2
{
public:
	static void copy(char a[], char b[]);
	static void copy2(char a[], char b[]);
	static int len(const char *s);
	static int compare(const char* s, const char* t);
};

